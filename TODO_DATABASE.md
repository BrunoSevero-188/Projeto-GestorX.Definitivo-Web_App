# TODO - Configurar DATABASE_URL (Prisma/Next)

- [ ] Confirmar como o projeto carrega variáveis de ambiente (ex: `.env` na raiz do `gestorx-web`).
- [ ] Criar/ajustar arquivo `.env` com o valor do `DATABASE_URL` informado.
- [ ] (Opcional) Garantir que `.env.example` contenha o mesmo formato para facilitar setup.
- [ ] Rodar `npm run prisma:generate` e `npm run build` (ou ao menos `npm run prisma:migrate`) para validar.
- [ ] Confirmar que o `schema.prisma` está usando a mesma datasource (via `DATABASE_URL`).

